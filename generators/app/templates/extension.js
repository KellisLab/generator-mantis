/** Runs in the extension host worker (not in the panel iframe). */
exports.activate = async function () {
  // register commands with mantis.commands.registerCommand, or mantis.events.subscribe, etc.
};

exports.deactivate = async function () {};
